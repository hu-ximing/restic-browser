# Project instructions

- Every bug fix must be the smallest change that resolves the root cause.
- Fixes must remain cross-platform across Windows, Linux, and macOS.
- Do not use a terminal-specific workaround when a portable behavior or graceful fallback exists.
